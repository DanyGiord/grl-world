---

---

<div class="w-full bg-[#FE939E] border-t-4
      border-black border-b-4">
  <div
    class="text-black flex justify-between items-center text-center h-16 text-xl sm:text-x2l moving_div"
  >
    <span>SALE</span>
    <span>
      <img src="./img/smile.png" alt="" class="w-8 h-8 smile" />
    </span>
    <span>GRL WORLD</span>
    <span>
      <img src="./img/smile.png" alt="" class="w-8 h-8 smile" />
    </span>
    <span>SALE</span>
    <span>
      <img src="./img/smile.png" alt="" class="w-8 h-8 smile" />
    </span>
    <span>SEA</span>
    <span>
      <img src="./img/smile.png" alt="" class="w-8 h-8 smile" />
    </span>
  </div>
</div>

<script>
  import { gsap } from "gsap";
  document.addEventListener("DOMContentLoaded", () => {
    const marqueeContent = document.querySelector(".marquee-content");
    const contentWidth = marqueeContent.scrollWidth / 2;

    gsap.to(".smile", {
      rotation: 720,
      duration: 3,
      ease: "none",
      repeat: -1,
      yoyo: true,
    });
    gsap.to(".marquee-content", {
      x: `-${contentWidth}px`, // Move the entire width of one instance of the content
      duration: 10, // Duration of the animation (adjust as needed)
      ease: "linear",
      repeat: -1, // Infinite repeat
      repeatDelay: 0, // No delay between repeats
    });
  });
</script>
